import React from 'react'
import { Modal } from 'react-bootstrap'

function NewConversationModal({ closeModal }) {
  return <Modal.Header> create Contact</Modal.Header>
}

export default NewConversationModal
